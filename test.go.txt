package main

import (
	"context"
	"fmt"
	"log"

	"github.com/tildezero/draftbot/pkg/draftout"
)

func main() {
	c := draftout.New()
	a, err := c.GetPlayerStats(context.Background(), "lightlaser", 1, draftout.FilterCompetitive)
	if err != nil {
		log.Fatalln("gps f")
	}
	fmt.Printf("%s, %d, %s\n", a.Player.Username, a.Player.Elo, a.Player.RankName)
}
